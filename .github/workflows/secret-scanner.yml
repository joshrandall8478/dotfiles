name: Secret Scanner
on:
  push:
    branches: ["main"]

  workflow_dispatch:

jobs:
  
  scan:
    name: Scan
    runs-on: ubuntu-latest
    steps:
      - name: TruffleHog OSS
        uses: trufflesecurity/trufflehog@v3.90.8
